# Git Conventions

- **Conventional commits**: Use `feat:`, `fix:`, `refactor:`, `docs:`, `chore:`.
- **Atomic commits**: One logical change per commit.
- **No secrets in git**: Use `.env.example` template, never commit `.env`.
- **Branch naming**: `feature/`, `fix/`, `refactor/` prefixes.
